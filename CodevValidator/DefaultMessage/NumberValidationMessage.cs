namespace CodevValidator.DefaultMessage
{
    internal static class NumberValidationMessage
    {
        public const string MINERROR = "{0} is less than {1}";
        public const string MAXERROR = "{0} is more than {1}";
    }
}
